@model IEnumerable<GestionSemillero1.Models.semillero>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Gestionar Semillero";
    string seccionActiva = ViewBag.SeccionActual;
    decimal idSemilleroSeleccionadoSeccion4 = ViewBag.IdSemilleroSeleccionado4 ?? 0;
}

<style>
    .btn-action-custom {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100%;
        padding: 10px 15px;
        border-radius: 8px;
        font-weight: 600;
        text-align: center;
        transition: all 0.3s ease;
    }

    .table-header-banner {
        padding: 15px;
        font-weight: 600;
        font-size: 1.1rem;
        display: flex;
        align-items: center;
        border-radius: .5rem .5rem 0 0;
    }

    .nav-tabs .nav-link {
        color: #333333 !important;
        border: none !important;
        text-decoration: none !important;
    }

        .nav-tabs .nav-link.active {
            background-color: #278a5f !important;
            color: #ffffff !important;
            box-shadow: 0 4px 6px rgba(39, 138, 95, 0.3);
        }

    .form-control-custom {
        border-radius: 6px;
        border: 1px solid #ced4da;
        padding: 0.5rem 0.75rem;
        width: 100%;
        transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
    }

        .form-control-custom:focus {
            border-color: #278a5f;
            outline: 0;
            box-shadow: 0 0 0 0.2rem rgba(39, 138, 95, 0.25);
        }

    .step-badge {
        display: inline-block;
        width: 24px;
        height: 24px;
        text-align: center;
        line-height: 24px;
        border-radius: 50%;
        font-size: 0.85rem;
        margin-right: 8px;
    }
</style>

<div class="container-fluid">

    <div class="mb-4 mt-2">
        <h3 class="fw-bold text-dark">
            <i class="bi bi-people-fill me-2" style="color: #278a5f;"></i>Módulo de Gestión de Semilleros
        </h3>
        <p class="text-muted small">Administre la información de los semilleros y gestione a los investigadores vinculados.</p>
    </div>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show mb-4" role="alert" style="border-radius: 8px;">
            <i class="bi bi-exclamation-triangle-fill me-2"></i>@TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="row">
        <!-- Contenedor principal al 100% -->
        <div class="col-12">

            <!-- Pestañas de Navegación -->
            <ul class="nav nav-tabs border-0 mb-4 bg-white p-2 shadow-sm" style="border-radius: 12px; gap: 10px;">
                <li class="nav-item">
                    <button class="nav-link border-0 fw-bold px-4 py-2 @(seccionActiva != "investigadores" ? "active" : "")"
                            id="btn-tab-semilleros" onclick="cambiarVistaPestaña('semilleros')" style="border-radius: 8px; transition: all 0.2s;">
                        <i class="bi bi-building me-2"></i>Gestión de Semilleros
                    </button>
                </li>
                <li class="nav-item">
                    <button class="nav-link border-0 fw-bold px-4 py-2 @(seccionActiva == "investigadores" ? "active" : "")"
                            id="btn-tab-investigadores" onclick="cambiarVistaPestaña('investigadores')" style="border-radius: 8px; transition: all 0.2s;">
                        <i class="bi bi-people-fill me-2"></i>Gestión de Investigadores
                    </button>
                </li>
            </ul>

            <div class="tab-content">

                <!-- PESTAÑA 1: GESTIÓN DE SEMILLEROS -->
                <div class="tab-pane fade @(seccionActiva != "investigadores" ? "show active" : "")">

                    @using (Html.BeginForm("GestionarSemillero", "Lider", FormMethod.Post, new { id = "formSemillero" }))
                    {
                        <div class="row">
                            <div class="col-md-6 mb-4">
                                <div class="card h-100 shadow-sm border-0 p-3">
                                    <h5 class="card-title mb-3 fw-bold"><span class="step-badge text-white" style="background-color: #278a5f;">1</span> Identificación</h5>
                                    <div class="mb-3">
                                        <input type="text" id="txtID" name="ID_semillero" class="form-control-custom" readonly style="background-color: #f8f9fa; cursor: not-allowed;" required placeholder="ID asignado" />
                                    </div>
                                    <div>
                                        <input type="text" id="txtNombre" name="nombre_semillero" class="form-control-custom" placeholder="Nombre del semillero" required />
                                    </div>
                                </div>
                            </div>

                            <div class="col-md-6 mb-4">
                                <div class="card h-100 shadow-sm border-0 p-3">
                                    <h5 class="card-title mb-3 fw-bold"><span class="step-badge text-white" style="background-color: #278a5f;">2</span> Clasificación</h5>
                                    <div class="mb-3">
                                        <input type="text" id="txtLinea" name="linea_investigacion" class="form-control-custom" placeholder="Línea de investigación" required />
                                    </div>
                                    <div>
                                        <input type="date" id="txtFecha" name="fecha_creacion_semillero" class="form-control-custom" required />
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="card mb-4 shadow-sm border-0 p-3">
                            <h5 class="card-title mb-3 fw-bold"><span class="step-badge text-white" style="background-color: #278a5f;">3</span> Descripción General</h5>
                            <textarea id="txtDescripcion" name="descripcion_semillero" class="form-control-custom" rows="3" placeholder="Escriba la descripción del semillero..."></textarea>
                        </div>

                        <div class="d-flex flex-wrap gap-2 mb-4">
                            <button type="submit" name="accion" value="actualizar" class="btn text-white px-4 fw-bold" style="background-color: #c98d26; border-radius: 8px;"><i class="bi bi-arrow-clockwise me-2"></i>Actualizar</button>
                            <button type="submit" name="accion" value="eliminar" class="btn text-white px-4 fw-bold" style="background-color: #d42346; border-radius: 8px;"><i class="bi bi-trash me-2"></i>Eliminar</button>
                            <button type="button" id="btnLimpiar" class="btn btn-secondary text-white px-4 fw-bold" style="border-radius: 8px;"><i class="bi bi-eraser me-2"></i>Limpiar</button>
                        </div>
                    }

                    <div class="table-responsive-container shadow-sm p-0 mb-4" style="border-radius: 8px; overflow: hidden; background-color: white;">
                        <div class="table-header-banner text-white d-flex justify-content-between align-items-center" style="background: linear-gradient(87deg, #278a5f 0, #278a5f 100%);">
                            <div><i class="bi bi-table me-2"></i>Semilleros del Sistema</div>
                            <small class="text-white-50 fw-normal"><i class="bi bi-info-circle me-1"></i>Haga clic en una fila para editar</small>
                        </div>
                        <div class="table-responsive p-3">
                            <table class="table table-hover align-middle mb-0" id="tablaSemilleros" style="cursor: pointer;">
                                <thead class="table-light">
                                    <tr>
                                        <th>ID</th>
                                        <th>Nombre</th>
                                        <th>Fecha Creación</th>
                                        <th>Línea de Investigación</th>
                                        <th>Estado</th>
                                        <th style="display:none;">Descripción Oculta</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model != null && Model.Any())
                                    {
                                        foreach (var item in Model)
                                        {
                                            <tr>
                                                <td class="fw-bold ID-celda">@item.ID_semillero</td>
                                                <td style="color: #32325d; font-weight: 600;">@item.nombre_semillero</td>
                                                <td>@(item.fecha_creacion_semillero != null ? item.fecha_creacion_semillero.ToString("yyyy-MM-dd") : "")</td>
                                                <td>@item.linea_investigacion</td>
                                                <td><span class="badge text-white p-2" style="background-color: #278a5f; border-radius:6px;">@item.estado</span></td>
                                                <td style="display:none;" class="descripcion-oculta">@item.descripcion_semillero</td>
                                            </tr>
                                        }
                                    }
                                    else
                                    {
                                        <tr>
                                            <td colspan="6" class="text-center p-4 text-muted">
                                                <i class="bi bi-info-circle me-2"></i> Aún no tienes semilleros asignados. Contacta al Administrador.
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>

                </div>

                <!-- PESTAÑA 2: GESTIÓN DE INVESTIGADORES -->
                <div class="tab-pane fade @(seccionActiva == "investigadores" ? "show active" : "")">

                    <div class="row">
                        <!-- La tabla ocupa todo el ancho (col-12) -->
                        <div class="col-12 mb-4">
                            <div class="table-responsive-container shadow-sm p-0 h-100" style="border-radius: 8px; overflow: hidden; background-color: white;">

                                <!-- Banner Superior -->
                                <div class="table-header-banner text-white d-flex justify-content-between align-items-center" style="background: linear-gradient(87deg, #278a5f 0, #278a5f 100%);">
                                    <div><i class="bi bi-people-fill me-2"></i>Miembros Vinculados: @(ViewBag.NombreSemilleroSeleccionado4 ?? "Todos")</div>
                                    <button type="button" id="btnAbrirModalAgregar" class="btn btn-sm btn-light fw-bold shadow-sm" style="color: #278a5f; border-radius: 6px;">
                                        <i class="bi bi-person-plus-fill me-1"></i> Agregar Investigador
                                    </button>
                                </div>

                                <div class="table-responsive p-3 border-bottom">
                                    <table class="table table-hover align-middle mb-0" id="tablaInvestigadores" style="cursor:pointer;">
                                        <thead class="table-light">
                                            <tr>
                                                <th>ID</th>
                                                <th>Nombre Completo</th>
                                                <th>Correo Institucional</th>
                                                <th>Estado Cuenta</th>
                                                <th style="display:none;">ID Usuario Oculto</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @if (ViewBag.InvestigadoresDeEsteSemillero != null && (ViewBag.InvestigadoresDeEsteSemillero as List<GestionSemillero1.Models.InvestigadorFiltroViewModel>).Any())
                                            {
                                                foreach (var inv in ViewBag.InvestigadoresDeEsteSemillero as List<GestionSemillero1.Models.InvestigadorFiltroViewModel>)
                                                {
                                                    <tr>
                                                        <td class="fw-bold">@inv.ID_investigador</td>
                                                        <td style="color: #32325d; font-weight: 600;">@inv.NombreCompleto</td>
                                                        <td>@inv.correo_usuario</td>
                                                        <td>
                                                            @if (inv.estado_usuario != null && (inv.estado_usuario.ToLower() == "activo" || inv.estado_usuario.ToLower() == "activa"))
                                                            {
                                                                <span class="badge text-white p-2" style="background-color: #278a5f; border-radius:6px;">@inv.estado_usuario</span>
                                                            }
                                                            else
                                                            {
                                                                <span class="badge bg-danger text-white p-2" style="border-radius:6px;">@inv.estado_usuario</span>
                                                            }
                                                        </td>
                                                        <td style="display:none;" class="id-user-oculto">@inv.ID_usuario</td>
                                                    </tr>
                                                }
                                            }
                                            else
                                            {
                                                <tr><td colspan="4" class="text-center text-muted p-5">Actualmente no hay investigadores registrados en este semillero.</td></tr>
                                            }
                                        </tbody>
                                    </table>
                                </div>

                                <!-- BARRA DE ACCIONES AL FINAL DE LA TABLA -->
                                <div class="bg-light p-3 d-flex flex-wrap justify-content-between align-items-center">
                                    <div class="text-muted small">
                                        <i class="bi bi-info-circle me-1"></i>
                                        <span id="infoSeleccion" class="fw-bold">Ningún investigador seleccionado en la tabla</span>
                                    </div>
                                    <div class="d-flex gap-2">
                                        <button type="button" onclick="ejecutarAccionInvestigador('habilitar')" class="btn btn-outline-success fw-bold bg-white" style="border-radius: 8px;">
                                            <i class="bi bi-person-check me-1"></i> Habilitar
                                        </button>
                                        <button type="button" onclick="ejecutarAccionInvestigador('deshabilitar')" class="btn btn-outline-warning text-dark fw-bold bg-white" style="border-radius: 8px;">
                                            <i class="bi bi-person-x me-1"></i> Deshabilitar
                                        </button>
                                        <button type="button" onclick="ejecutarAccionInvestigador('eliminar')" class="btn btn-outline-danger fw-bold bg-white" style="border-radius: 8px;">
                                            <i class="bi bi-trash me-1"></i> Eliminar
                                        </button>
                                    </div>
                                </div>

                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>

<!-- FORMULARIO OCULTO PARA PROCESAR LAS ACCIONES DE INVESTIGADORES -->
<form id="formAccionesFila" action="@Url.Action("ProcesarAccionesInvestigadores", "Lider")" method="post" style="display:none;">
    <input type="hidden" id="hiddenFilaSemilleroId" name="idSemilleroSeleccionado" value="@idSemilleroSeleccionadoSeccion4" />
    <input type="hidden" id="hiddenFilaUsuarioId" name="idUsuarioSeleccionado" value="" />
    <input type="hidden" id="hiddenFilaAccion" name="accionInvestigador" value="" />
</form>

<!-- Modal Agregar Investigador -->
<div class="modal fade" id="modalAgregarInvestigador" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content" style="border-radius:12px; overflow: hidden; border: none;">
            <div class="modal-header text-white" style="background-color: #278a5f;">
                <h5 class="modal-title fw-bold text-white"><i class="bi bi-person-plus-fill me-2"></i>Asignar Nuevo Investigador</h5>
                <button type="button" class="btn-close btn-close-white" id="btnCerrarModalX" aria-label="Close"></button>
            </div>
            @using (Html.BeginForm("ProcesarAccionesInvestigadores", "Lider", FormMethod.Post))
            {
                <div class="modal-body p-4">
                    <input type="hidden" name="idSemilleroSeleccionado" value="@idSemilleroSeleccionadoSeccion4" />
                    <input type="hidden" name="accionInvestigador" value="agregar" />

                    <p class="small text-muted mb-3">Vinculando personal al grupo ID: <strong style="color: #278a5f;">@idSemilleroSeleccionadoSeccion4</strong></p>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label small fw-bold">Correo Institucional</label>
                            <input type="email" name="nuevoInvestigadorCorreo" class="form-control-custom" placeholder="Ej: daniel@sena.edu.co" required />
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label small fw-bold">Contraseña Inicial</label>
                            <input type="password" name="nuevoInvestigadorContrasena" class="form-control-custom" placeholder="Asigne clave" required />
                        </div>
                    </div>

                    <hr class="my-2" style="color: #dee2e6;" />

                    <div class="mb-3">
                        <label class="form-label small fw-bold">Nombres</label>
                        <input type="text" name="nuevoInvestigadorNombre" class="form-control-custom" placeholder="Nombres completos" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label small fw-bold">Apellidos</label>
                        <input type="text" name="nuevoInvestigadorApellido" class="form-control-custom" placeholder="Apellidos completos" required />
                    </div>
                    <div class="row">
                        <div class="col-6 mb-3">
                            <label class="form-label small fw-bold">Tipo Doc.</label>
                            <select name="nuevoInvestigadorDoc" class="form-control-custom" required>
                                <option value="CC">C.C.</option>
                                <option value="TI">T.I.</option>
                            </select>
                        </div>
                        <div class="col-6 mb-3">
                            <label class="form-label small fw-bold">Edad</label>
                            <input type="number" name="nuevoInvestigadorEdad" class="form-control-custom" placeholder="Edad" min="15" max="90" required />
                        </div>
                    </div>
                    <div class="mb-2">
                        <label class="form-label small fw-bold">Teléfono Celular</label>
                        <input type="text" name="nuevoInvestigadorTel" class="form-control-custom" placeholder="Número de contacto" required />
                    </div>
                </div>
                <div class="modal-footer bg-light p-3 border-0">
                    <button type="button" class="btn btn-secondary btn-sm px-4 fw-bold" id="btnCerrarModalCancel" style="border-radius: 6px;">Cancelar</button>
                    <button type="submit" class="btn text-white btn-sm fw-bold px-4" style="background-color: #278a5f; border-radius: 6px;">Guardar Registro</button>
                </div>
            }
        </div>
    </div>
</div>

<script>
    function cambiarVistaPestaña(tipo) {
        if(tipo === 'semilleros') {
            window.location.href = `@Url.Action("GestionarSemillero", "Lider")?seccionCargada=semilleros`;
        } else if(tipo === 'investigadores') {
            window.location.href = `@Url.Action("GestionarSemillero", "Lider")?seccionCargada=investigadores`;
        }
    }

    // Procesar acción enviando el formulario oculto
    function ejecutarAccionInvestigador(accion) {
        const idUsuario = document.getElementById("hiddenFilaUsuarioId").value;
        const idSemillero = document.getElementById("hiddenFilaSemilleroId").value;

        if (!idSemillero || idSemillero === "0") {
            alert("No se ha identificado el semillero al que pertenece este investigador.");
            return;
        }

        if (!idUsuario) {
            alert("Por favor seleccione un investigador haciendo clic sobre su fila en la tabla antes de continuar.");
            return;
        }

        let verboAccion = accion === 'eliminar' ? 'eliminar del semillero a' : accion;

        if (confirm(`¿Está seguro de que desea ${verboAccion} este investigador?`)) {
            document.getElementById("hiddenFilaAccion").value = accion;
            document.getElementById("formAccionesFila").submit();
        }
    }

    document.addEventListener("DOMContentLoaded", function () {
        const tablaSemilleros = document.getElementById("tablaSemilleros");
        const txtID = document.getElementById("txtID");
        const txtNombre = document.getElementById("txtNombre");
        const txtLinea = document.getElementById("txtLinea");
        const txtFecha = document.getElementById("txtFecha");
        const txtDescripcion = document.getElementById("txtDescripcion");
        const btnLimpiar = document.getElementById("btnLimpiar");

        const tablaInvestigadores = document.getElementById("tablaInvestigadores");
        const infoSeleccion = document.getElementById("infoSeleccion");
        const hiddenUsuarioId = document.getElementById("hiddenFilaUsuarioId");

        const btnAbrirModal = document.getElementById("btnAbrirModalAgregar");
        const modalElement = document.getElementById("modalAgregarInvestigador");
        const btnCerrarX = document.getElementById("btnCerrarModalX");
        const btnCerrarCancel = document.getElementById("btnCerrarModalCancel");

        // Modal Agregar
        if (btnAbrirModal) {
            btnAbrirModal.addEventListener("click", function () {
                const idSemillero = document.getElementById("hiddenFilaSemilleroId").value;
                if (idSemillero === "0" || idSemillero === "") {
                    alert("Asegúrese de estar consultando un semillero válido antes de agregar investigadores.");
                    return;
                }
                modalElement.classList.add("show");
                modalElement.style.display = "block";
                document.body.classList.add("modal-open");
            });
        }

        function cerrarModal() {
            modalElement.classList.remove("show");
            modalElement.style.display = "none";
            document.body.classList.remove("modal-open");
        }
        if (btnCerrarX) btnCerrarX.addEventListener("click", cerrarModal);
        if (btnCerrarCancel) btnCerrarCancel.addEventListener("click", cerrarModal);

        // Selección de filas Semilleros (Pestaña 1)
        if (tablaSemilleros) {
            tablaSemilleros.querySelectorAll("tbody tr").forEach(fila => {
                fila.addEventListener("click", function () {
                    if (this.querySelector(".ID-celda")) {
                        tablaSemilleros.querySelectorAll("tbody tr").forEach(r => r.style.backgroundColor = "");
                        this.style.backgroundColor = "rgba(39, 138, 95, 0.08)";

                        txtID.value = this.cells[0].innerText.trim();
                        txtNombre.value = this.cells[1].innerText.trim();

                        let fechaTexto = this.cells[2].innerText.trim();
                        if(fechaTexto.length > 10) fechaTexto = fechaTexto.substring(0, 10);
                        txtFecha.value = fechaTexto;

                        txtLinea.value = this.cells[3].innerText.trim();
                        txtDescripcion.value = this.querySelector(".descripcion-oculta").innerText.trim();
                    }
                });
            });
        }

        if (btnLimpiar) {
            btnLimpiar.addEventListener("click", function () {
                if(tablaSemilleros) {
                    tablaSemilleros.querySelectorAll("tbody tr").forEach(r => r.style.backgroundColor = "");
                }
                txtID.value = "";
                txtNombre.value = "";
                txtLinea.value = "";
                txtFecha.value = "";
                txtDescripcion.value = "";
            });
        }

        // Recuperar la lógica de selección de Investigadores (Pestaña 2)
        if (tablaInvestigadores) {
            tablaInvestigadores.querySelectorAll("tbody tr").forEach(fila => {
                fila.addEventListener("click", function () {
                    if (this.querySelector(".id-user-oculto")) {
                        tablaInvestigadores.querySelectorAll("tbody tr").forEach(r => r.style.backgroundColor = "");
                        this.style.backgroundColor = "rgba(39, 138, 95, 0.12)";

                        const idUser = this.querySelector(".id-user-oculto").innerText.trim();
                        const nombreCompleto = this.cells[1].innerText.trim();

                        hiddenUsuarioId.value = idUser;
                        infoSeleccion.innerHTML = `Seleccionado: <span style="color: #278a5f;">${nombreCompleto}</span>`;
                    }
                });
            });
        }
    });
</script>